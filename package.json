{
  "name": "taiyaki",
  "version": "1.0.0",
  "description": "",
  "main": "hardhat.config.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "npm run clean && hardhat test",
    "build": "npm run clean && hardhat compile --network optimism",
    "deploy": "hardhat run scripts/TAIYAKI.deploy.js --network optimism",
    "lint": "prettier-standard --format",
    "clean": "rimraf cache artifacts"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@eth-optimism/plugins": "0.0.4",
    "@nomiclabs/hardhat-ethers": "2.0.1",
    "dotenv": "8.2.0",
    "hardhat": "2.0.8",
    "prettier-standard": "16.4.1",
    "rimraf": "3.0.2"
  },
  "dependencies": {
    "chai": "^4.3.0",
    "web3": "^1.3.4"
  }
}
